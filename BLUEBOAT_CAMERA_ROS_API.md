# BlueBoat camera ROS 2 API

See `BLUEBOAT_CAMERA_WEB.md` for build, launch, website, RViz, service, and
Foxglove instructions. Camera mode is numeric (`1` through `4`), the camera pod
mode is removed, and published image topics use `/<boat>/camera/image`.
